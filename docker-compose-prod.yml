version: "3"
services:
  client:
    build:
      context: client
      dockerfile: Dockerfile.prod
    ports:
      - "80:80"
  server:
    build:
      context: server
      dockerfile: Dockerfile.prod
